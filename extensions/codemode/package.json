{
  "name": "@ziggy/codemode",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Confined JavaScript orchestration over Profile-declared MCP stdio tools.",
  "keywords": ["pi-package"],
  "scripts": {
    "build": "bun build index.ts --target=bun --format=esm --minify --banner='/* oxlint-disable -- generated self-contained runtime bundle */' --outfile=dist/index.js --external @earendil-works/pi-coding-agent --external typebox",
    "test": "bun test ./test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "oxlint index.ts src test",
    "fmt": "oxfmt index.ts src test"
  },
  "pi": {
    "extensions": ["./dist/index.js"]
  },
  "dependencies": {
    "acorn": "8.15.0",
    "effect": "4.0.0-beta.99"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1",
    "typebox": "1.3.7"
  }
}
