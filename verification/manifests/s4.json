{
  "schemaVersion": 1,
  "stage": "s4",
  "status": "pending",
  "predecessors": ["s0", "s1", "s2", "s3"],
  "scenarios": [
    "s4.extension-daemon-lifecycle",
    "s4.extension-journal-recovery",
    "s4.extension-mutation-recovery",
    "s4.extension-tool-boundary",
    "s4.manifest-version-compatibility"
  ],
  "gates": ["manifest-integrity", "extension-integrity", "fmt-check", "typecheck", "scenarios"],
  "requirements": [
    {
      "id": "s4.manifest-version-contract",
      "status": "implemented",
      "description": "Extension manifests strictly decode duplicate-free JSON, validate canonical SemVer 2 versions and Ziggy range grammar, exclude provenance claims, and expose deterministic build-aware and prerelease-clause compatibility without claiming install/load integration or S4 stage completion."
    },
    {
      "id": "s4.merlin-ledger-integrity",
      "status": "implemented",
      "description": "The closed 47-candidate Merlin migration ledger, conditional independent-review set, review freshness and budget contract, and deterministic extension-integrity gate are checked in without requiring the sibling Merlin repository."
    },
    {
      "id": "s4.extension-daemon-lifecycle",
      "status": "implemented",
      "description": "The production daemon owns Extension install, exact approval, disabled installation, list, enable, doctor, disable, and restart persistence over the real Unix attach socket while Client operations preserve direct-write canaries."
    },
    {
      "id": "s4.extension-mutation-recovery",
      "status": "implemented",
      "description": "Tool-list and Skill-load seal failures durably invalidate approvals, restored immutable bytes remain rejected, explicit fresh approval reinstall recovers disabled without moving mutable state, and Extension processes receive only declared environment values."
    },
    {
      "id": "s4.extension-tool-boundary",
      "status": "implemented",
      "description": "Approved compatible enabled Tools are imported only from mode-0700 scoped execution snapshots after exact authority, path, digest, and live-seal validation; ABI, identity, schema, output, collision, mutation, and compiled post-build TypeScript boundaries fail closed."
    },
    {
      "id": "s4.extension-journal-recovery",
      "status": "implemented",
      "description": "Schema-stamped Extension publication journals recover real process death to a complete old or new generation at every durable cut point, preserve mutable-state inode identity, clean idempotently, and serialize list discovery across the package publication gap."
    }
  ]
}
